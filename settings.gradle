rootProject.name = 'file-generator-platform'

include ':file-generation-system'

project(':file-generation-system').projectDir = file('service')

project(':file-generation-system').name = 'file-generation-service'
